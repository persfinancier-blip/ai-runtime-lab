# Test Worker

## Task

This is a harmless connectivity test for the AI runtime worker.

Do not modify application code.

Create a file named `worker-test-result.txt` at the repository root containing exactly:

WORKER_OK

Then commit the change with a clear commit message.

## Acceptance criteria

- `worker-test-result.txt` exists at repository root.
- Its content is exactly `WORKER_OK`.
- No other application files are changed.
