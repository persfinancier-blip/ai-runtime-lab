# LAB-086 — asymmetric break-glass history

Reference slice proving a legacy-HMAC cutoff followed by Ed25519 threshold break-glass proofs whose durable verification state contains public keys and signatures only. Private signing capability stays runtime-only.

This is an HSM/KMS-compatible interface boundary; no live HSM/KMS is claimed.
